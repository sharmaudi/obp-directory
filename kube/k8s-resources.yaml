apiVersion: v1
kind: Service
metadata:
  name: obp-remote-consent-service-v2
  labels:
    app: obp-remote-consent-service-v2
spec:
  type: NodePort
  ports:
  - name: http
    port: 8080
    protocol: TCP
    targetPort: 8000
    nodePort: 31959
  selector:
    app: obp-remote-consent-service-v2
    version: v1
---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: obp-remote-consent-service-v2
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: obp-remote-consent-service-v2
        version: v1
    spec:
      hostAliases:
        - ip: 127.0.0.1
          hostnames:
            - bank-as.dpe-opendata.com
      containers:
      - args:
          - gunicorn
          - -c
          - python:config.gunicorn
          - rcs.app:create_app()
        env:
        - name: PYTHONUNBUFFERED
          value: "1"
        name: obp-remote-consent-service-v2
        image: gcr.io/dpe-obp-mgmt-services-1badd717/obp-remote-consent-service-v2:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8000
        livenessProbe:
          httpGet:
            path: /_status/health
            port: 8000
          initialDelaySeconds: 30
          periodSeconds: 3
        readinessProbe:
          httpGet:
            path: /_status/health
            port: 8000
          initialDelaySeconds: 40
          periodSeconds: 5
        resources:
          requests:
            memory: "250Mi"
            cpu: "250m"
          limits:
            memory: "500Mi"
            cpu: "500m"
---
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
    nginx.org/proxy-buffers: "128k"
    nginx.org/proxy-buffer-size: "4 256k"
    nginx.ingress.kubernetes.io/proxy-body-size: 500m
    ingress.kubernetes.io/proxy-body-size: 500m
  labels:
    app: obp-remote-consent-service-v2
  name: obp-remote-consent-service-v2
spec:
  tls:
    - hosts:
      - rcs.dpe-opendata.com
      secretName: rcs.dpe-opendata.com
  rules:
    - host: rcs.dpe-opendata.com
      http:
        paths:
        - backend:
            serviceName: obp-remote-consent-service-v2
            servicePort: 8080
          path: /*